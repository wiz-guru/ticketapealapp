# The core app needs NO environment variables.
#
# OPTIONAL — only for the "Polish with AI" button on the result step:
# Get a key at console.anthropic.com and add it in Vercel → Settings → Environment Variables.
ANTHROPIC_API_KEY=
# Optional model override (default: claude-sonnet-4-6):
# POLISH_MODEL=claude-sonnet-4-6
